function addition(num, num2) {
    if (Number.isInteger(num && num2)){
        return num + num2;
    } else {
        console.log(Error);
        return Error;
    }
    
}

module.exports = addition;